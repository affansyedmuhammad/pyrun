# Copy to .env (gitignored). Loaded into ENV in development only.
# Every setting is documented in docs/DESIGN.md §9; bin/rails pyrun:config prints what is in effect.
ALLOWED_EMAILS=you@example.com
ADMIN_EMAILS=you@windbornesystems.com
# SANDBOX_TIMEOUT_SECONDS=10
